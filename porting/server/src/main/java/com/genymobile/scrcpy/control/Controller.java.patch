--- /var/folders/hr/xw76b5ps73n6t_4n674pt1qc0000gn/T/tmp.xvOoOPpUOl	2026-05-23 22:20:04
+++ server/src/main/java/com/genymobile/scrcpy/control/Controller.java	2026-05-23 22:19:50
@@ -241,7 +241,13 @@
 
         boolean alive = true;
         while (!Thread.currentThread().isInterrupted() && alive) {
-            alive = handleEvent();
+            try {
+                alive = handleEvent();
+            } catch (RuntimeException e) {
+                // Mark: swallow per-event runtime exceptions to keep the session alive
+                // (e.g. SecurityException from ClipboardService on some rooted ROMs).
+                Ln.e("Controller handleEvent failed, continue", e);
+            }
         }
     }
 
@@ -839,9 +845,7 @@
                 if (timeout < 0) {
                     return null;
                 }
-                if (timeout > 0) {
-                    displayDataAvailable.wait(timeout);
-                }
+                displayDataAvailable.wait(timeout);
                 data = displayData.get();
             }
 
